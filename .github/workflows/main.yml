name: 🧪

on: [issues, pull_requests]

jobs:
  main:
    runs-on: ubuntu-latest
    permissions:
      issues: write

    steps:
    - uses: actions/checkout@v2
    - name: Run Action
      uses: ./
